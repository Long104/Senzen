# worklog

### [2026-09-24] [Pipeline] feat/landing-refresh: feat: replace ai demo narrative with real screenshots and honest copy

**Problem**: Landing page sold a fake product (AI chat planner demo, Family Trip $2000 auto-save block, fake stats band, "Meet Senzen" hype marquee, $ currency in a ฿ app).

**Changes**: Deleted ai-plan-demo.tsx + HeroStatsBand. Rewrote hero (32px inline bear before eyebrow `hi, this is senzen`, tagline `your money, remembered.`, browser-framed real home.png). New screenshot-section (home/report/plans cards, copy Set A captions) + shared browser-frame component. Honest FAQ (copy §5 verbatim), lowercase headings, single #1EC072 green, ฿ amounts, honest pricing/layout metadata, 2 mascot PNGs (128px).

**Verification**: tsc --noEmit exit 0; npm run build green; smoke test passes (3 pre-existing home.test.tsx QueryClient failures = baseline, untouched); grep gates zero (banned colors, uppercase tracking, auto-save/nudge/family trip/meet senzen, literal $); QA APPROVED desktop+mobile, console clean; independent review APPROVED (2 rounds); co-founder intent check APPROVED (3 rounds: Meet marquee, $412, Title Case, PNG size). Production cleanup: 22 seeded txns (IDs 21–42) + 2 seeded plans (11, 12) deleted via API; survivors verified (japan trip plan + 2 pre-existing txns intact).

**Outcome**: Committed 4f7b94e on feat/landing-refresh (worktree .worktrees/landing-refresh). Not pushed, not merged — awaiting Entrepreneur merge decision. Note: reviewer/co-founder agent types returned model-config 400s; general-agent fallbacks used for independent verification.

### [2026-09-24] [Quick Mode] feat/landing-refresh: feat: honest auth pages, minimal nav, kill pricing

**Problem**: Auth pages used uppercase titles, generic copy, no loading state. Navbar had dead links (/#features, /#strategies, /pricing, /createPlan). Footer had fake 4-column corporate grid with all # links. Pricing page was a fake marketing artifact.

**Changes**: Rewrote sign-in page (paper grid canvas, white centered card, Google/GitHub OAuth preserving hrefs, email/password form with in-flight loading state, error banner, lowercase copy, bear cameo, no Footer). Rewrote sign-up page (matching design, 4-field form, password-match feedback, server error display, OAuth buttons, no Footer). Navbar: removed announcement bar + dead center links, kept brand wordmark + sign in/get started only. Footer: slim single-row with sleeping bear, sign in/sign up, copyright — deleted fake 4-column grid. Deleted `pricing/` directory + purged all /pricing refs. Purged pricingItems from navbar-menu.tsx. Design-taste enforced: lowercase, no gradients, #1EC072 accent only, hover=subtle, active:scale-[0.98].

**Verification**: tsc --noEmit exit 0 (after .next cache clean); grep "pricing" in src/*.tsx = 1 commented-out dead code only (navbar-menu.tsx, no functional reference); pricing directory deleted.

**Outcome**: Committed 86b8135 on feat/landing-refresh. Not pushed, not merged.

### 2026-09-24 [Quick Mode] feat: bear favicon set
- Problem: favicon was baht-mark; user wants the bear brand mark everywhere.
- Changes: regenerated favicon.ico (256), app icon.png (192), apple-icon.png (180), public icon-192/icon-512 (PWA manifest) from public/logo.png bear.
- Verification: sips confirms all dimensions; ico format valid.
- Outcome: bear now identifies senzen in tabs, bookmarks, ios home screen, and pwa install.
